var angularApp = angular.module('app', []);


// for module creation
// angular.module('moduleName', [dependencies])

// for controller creation
// angular.controller('controllerName', [dependecies, controllerFunction]);

// for service creation
// angular.service('serviceName', [dependencies, serviceFunction])
